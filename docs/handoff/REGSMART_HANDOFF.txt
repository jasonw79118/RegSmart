REGSMART HANDOFF
Version: 04.06.2026.0004

Current direction
- Build RegSmart as a cloud-based Salesforce-style compliance operating system for banks.
- Start with full enterprise structure from day one.
- Keep everything on main for this project workflow.
- Deliver build work as flat ZIP files extracted at the repo root.
- Keep code out of chat.
- Include handoff documents when phases get larger or when moving chats.
- Keep the visible UI version in the bottom-left using MM.DD.YYYY.####.
- Expand the Help & User Guide page continuously as features are added.

What this version adds
- Protected routes with demo sign-in behavior.
- Enterprise entity selector in the header for scoped views.
- Dashboard updated to reflect selected entity context.
- Issues tracker filters for status, severity, and department.
- Issue detail page now includes lifecycle tracker, root cause, corrective action, evidence, and timeline.
- Audit detail page now shows requested evidence and linked issues.
- Evidence Center workspace added as a first-class left-nav item.
- Help page updated for the new behavior.

Important build rules going forward
- ZIP filename must match the version number exactly.
- ZIP contents must extract directly at the repo root, not into an extra nested folder.
- Every release must update the visible UI version, README, help page release text, and this handoff file.
- The Help page should increasingly function as the system user guide.

Suggested next phase
- Persist auth and selected entity state.
- Add issue creation and edit forms.
- Add evidence upload persistence and file metadata handling.
- Start backend models for entities, departments, issues, reviews, and users.
- Add workflow settings and status governance in admin.
- Build the first AI intake hooks for future masking and review.
