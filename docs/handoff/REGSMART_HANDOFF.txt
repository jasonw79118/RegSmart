REGSMART HANDOFF
Version: 04.06.2026.0009

Purpose of this pack:
Restore the live GitHub Pages site so it shows an actual interface instead of README text.

What changed:
- Added repo-root index.html
- Added styles.css
- Added .nojekyll
- Kept version in bottom-left using full-year format
- Embedded Help / User Guide directly in the visible shell

Why this was needed:
The live Pages site was serving repository text content rather than the interface. This hotfix makes the root publish path display a working shell immediately.

Next build target:
- carry this visual shell back into the fuller app structure
- issue create/edit forms
- audit detail workflows
- evidence metadata and privacy classes
- preserve bottom-left version on every release
- continue expanding Help / User Guide continuously

User preferences to preserve:
- deliver build work as zip files
- do not bury zip contents an extra level
- zip file name should match version number
- include handoff documents when phases get larger or when moving chats
- do not put build code in chat
