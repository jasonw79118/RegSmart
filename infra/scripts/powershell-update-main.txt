Pull the latest changes on main, then copy this version into the repo root and commit.

Suggested flow:
- git pull origin main
- extract the ZIP into the repo root
- git status
- git add .
- git commit -m "Update RegSmart to 04.06.2026.0006"
- git push origin main
